export type CostInput = {
  periodo: string;
  categoria: string;
  valor: number;
};

export type VolumeInput = {
  periodo: string;
  volume: number;
};

export type EventInput = {
  evento: string;
  data: string;
  descricao: string;
};
